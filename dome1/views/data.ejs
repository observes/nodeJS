<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ -->
    <title>数据展示</title>
    <!-- Bootstrap -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
    <script src="https://cdn.bootcss.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://cdn.bootcss.com/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body>
    <!--导航条-->
    <% include nav %>
    <!--主体部分-->
    <div class="container-fluid">
        <!--文件显示-->
        <div class="row">
            <% for(var i=0;i<recordList.length;i++){ %>
            <div class="panel panel-default col-lg-3  col-sm-6">
                <div class="panel-heading">
                    <h3 class="panel-title"><%= recordList[i].teacherField %></h3>
                </div>
                <div class="panel-body">
                    <%- recordList[i].teacherContent %>
                    <%= recordList[i].teacherType %>
                    <%= recordList[i].id %>
                    <p><%= recordList[i].teacherField %></p>
                    <p><%= recordList[i].teacherName %></p>
                    <p><%= recordList[i].teacherTitle %></p>
                    <%- recordList[i].teacherAchievement %>
                    <img src="<%= recordList[i].teacherImg %>" alt="">
                    <img src="<%= recordList[i].teacherHeadImg %>" alt="">
                </div>
            </div>
            <% } %>
        </div>
<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
<script src="https://cdn.bootcss.com/jquery/1.12.4/jquery.min.js"></script>
<!-- Include all compiled plugins (below), or include individual files as needed -->
<script src="/js/bootstrap.min.js"></script>
</body>
</html>