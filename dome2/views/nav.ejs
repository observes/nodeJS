<nav class="navbar navbar-default navbar-static-top navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <% if(user) { %>
                     <a class="navbar-brand" href="/u/<%= user %>">欢迎！！<%= user %></a>
            <% } else {%>
                        <a class="navbar-brand" href="javascript:">欢迎！！游客</a>
            <% } %>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li <% if(current==0){ %> class="active" <% } %>  ><a href="/">主页<span class="sr-only">(current)</span></a></li>
            </ul>
                <form class="navbar-form navbar-left row" method="post" action="/search">
                    <div class="form-group">
                        <input type="text" class="form-control" placeholder="关键词" name="key">
                    </div>
                    <button type="submit" class="btn btn-defaul ">搜索</button>
                </form>
            <ul class="nav navbar-nav navbar-right">
                <% if(user) { %>
                     <li <% if(current==3){ %> class="active" <% } %> ><a href="/post">发表</a></li>
                     <li <% if(current==4){ %> class="active" <% } %> ><a href="/upload">上传</a></li>
                     <li><a href="/logout">注销</a></li>
                <%} else { %>
                    <li  <% if(current==2){ %> class="active" <% } %> ><a href="/login">登陆</a></li>
                     <li  <% if(current==1){ %> class="active" <% } %>   ><a href="/reg">注册</a></li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
<h1 class="text-center text-danger"><%= title %></h1>